# Backend Email Service Environment Variables

# ===========================================
# Server Configuration
# ===========================================
PORT=3001
CORS_ORIGIN=http://localhost:8080

# ===========================================
# API Key Configuration
# ===========================================
# Option 1: Use a JSON file (recommended for production)
# Copy api-keys.example.json to api-keys.json and configure your keys
API_KEYS_FILE=./api-keys.json

# Option 2: Use environment variable (for Docker/containers)
# API_KEYS='[{"id":"key1","name":"Admin","key":"your-key-here","permission":"admin","active":true}]'

# Option 3: Legacy single key (backwards compatible)
# API_KEY=your-single-api-key

# Rate limiting (requests per minute per IP)
RATE_LIMIT_MAX=30

# ===========================================
# SMTP Configuration
# ===========================================
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@seudominio.com
SMTP_FROM_NAME=Sistema de Migração
